{
	"name": "axx",
	"version": "0.1.1-dev.1",
	"description": "async shell exec, streams, piping",
	"main": "dist",
	"scripts": {
		"prepare": "rm -rf dist && tsc && npm test -s",
		"build-debug": "rm -rf dist && tsc --sourceMap false --inlineSourceMap true",
		"test": "jest"
	},
	"keywords": [
		"async",
		"shell",
		"exec",
		"pipe"
	],
	"author": "Chase Moskal <chasemoskal@gmail.com>",
	"license": "ISC",
	"dependencies": {},
	"devDependencies": {
		"@types/jest": "22.2.2",
		"@types/node": "9.6.1",
		"jest": "22.4.3",
		"typescript": "2.8.1"
	}
}
